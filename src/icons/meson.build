foreach icon: icons
  install_data(
    'index.theme',
    install_dir: icon['dir'],
  )
  install_subdir(
    'scalable',
    install_dir: icon['dir'],
  )
  install_subdir(
    'scalable-max-32',
    install_dir: icon['dir'],
  )
endforeach
subdir('apps')
