font_dir = join_paths(get_option('datadir'), 'fonts', 'truetype')
install_dir =  join_paths(font_dir, meson.project_name())

font_sources = [
  'StarLabs-Regular.ttf',
]

install_data(font_sources, install_dir: install_dir)
